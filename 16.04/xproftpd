service ftp
{
  disable     = yes
  flags       = REUSE
  socket_type = stream
  wait        = no
  user        = root
  server      = /usr/sbin/in.proftpd
  server_args = -c /etc/proftpd/proftpd.conf
}
